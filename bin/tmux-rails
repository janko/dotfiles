#!/bin/bash

if [[ -z $TMUX ]]; then
  tmux new-session -d
fi

tmux new-window -n "console" "rails c"
tmux new-window -n "server" "rails s"
tmux new-window -n "code" "vim"

if [[ -z $TMUX ]]; then
  tmux attach
fi
